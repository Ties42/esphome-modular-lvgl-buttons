---
# Label only
# vars:
  # uid:
  # height:
  # text:
  # icon:
  # entity_id:
height: ${height}
width: ${width}
bg_opa: TRANSP
checkable: no
id: button_${uid}
widgets:
  - label:
      id: label_${uid}
      text_font: roboto36
      align: center
      text: ${text}
      text_color: 0x000000